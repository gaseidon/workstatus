        <form action="{{ route('profile.update') }}" method="POST" enctype="multipart/form-data">
                <a href="{{ route('profile.show') }}" class="text-gray-600 dark:text-gray-400 hover:text-gray-800 dark:hover:text-gray-200">
                    Отмена
                </a>
